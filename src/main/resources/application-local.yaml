spring:
  application:
    name: st_francis_of_assisi_management_system

  datasource:
    url: jdbc:mysql://localhost:3306/st_francis_db?useSSL=false&serverTimezone=UTC&useLegacyDateTimeCode=false
    username: root
    password: Password_1
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect

server:
  servlet:
    session:
      timeout: 30m